---
apiVersion: platform.confluent.io/v1beta1
kind: Connect
metadata:
  name: connect
  namespace: upgrade
spec:
  replicas: 1
  image:
    application: confluentinc/cp-server-connect:7.5.0
    init: confluentinc/confluent-init-container:2.8.0
  dependencies:
    kafka:
      bootstrapEndpoint: kafka.upgrade.svc.cluster.local:9092
    schemaRegistry:
      url: https://schemaregistry.upgrade.svc.cluster.local:8081
---
